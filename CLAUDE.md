# Orchestra — Quotes API

A tiny Express API serving quotes from a JSON file.

## Stack
- TypeScript + Express
- Vitest for tests
- No database — quotes live in src/quotes.json

## Commands
- `npm run dev` — start the server on http://localhost:3000
- `npm test` — run vitest
- `npm run lint` — typecheck (tsc --noEmit)
